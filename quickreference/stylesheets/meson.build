message('quickreference: stylesheets subdir')

qrconfig = configuration_data()

keys_svg = configure_file(
  input : 'keys-svg.xsl',
  output: 'keys-svg.xsl',
  copy: true,
)

keys_docbook = configure_file(
  input : 'keys-docbook.xsl',
  output: 'keys-docbook.xsl',
  copy: true,
)
