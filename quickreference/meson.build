message('quickreference subdir')

keys_xml = files('gimp-keys.xml')

subdir('stylesheets')
subdir('po')

# Generate translated xml files
subdir('xml')
# Convert to svg
subdir('svg')
# Convert to pdf
subdir('pdf')
