<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE variablelist PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
                      "http://www.docbook.org/xml/4.3/docbookx.dtd">
<!--
  This file is meant to be included by export dialogs that have all the
  metadata export settings: Exif, IPTC, XMP, color profile, thumbnail.
  (Comment will be handled separately.)

  We don't want to include <variablelist>, but only the child elements,
  so include it like this:
      <xi:include href="common-metadata-settings.xml"
        xpointer="xpointer(//variablelist/*)" />
-->

<variablelist>
  <varlistentry>
    <term>Metadata (edit)</term>
    <listitem>
      <para>
        You can click the <quote>(edit)</quote> link to open the
        <link linkend="plug-in-metadata-editor">metadata editor</link>
        to change or add any metadata that you want to be
        included in the image. Note: you will also still need to
        enable the relevant metadata saving options listed below.
      </para>
      <para>
        The metadata settings in the export dialog override the default values
        set in your <link linkend="gimp-metadata-export-policies">Metadata
        Export Preferences</link>.
      </para>
    </listitem>
  </varlistentry>
  <varlistentry>
    <term>Save Exif data</term>
    <listitem>
      <para>
        When this option is enabled, EXIF metadata will be saved
        in the exported image.
      </para>
    </listitem>
  </varlistentry>
  <varlistentry>
    <term>Save IPTC data</term>
    <listitem>
      <para>
        When this option is enabled, IPTC metadata will be saved
        in the exported image.
      </para>
    </listitem>
  </varlistentry>
  <varlistentry>
    <term>Save XMP data</term>
    <listitem>
      <para>
        When this option is enabled, XMP metadata will be saved
        in the exported image.
      </para>
    </listitem>
  </varlistentry>
  <varlistentry>
    <term>Save color profile</term>
    <listitem>
      <para>
        When this option is enabled, the color profile will be saved in
        the exported image.
      </para>
    </listitem>
  </varlistentry>
  <varlistentry>
    <term>Save thumbnail</term>
    <listitem>
      <para>
        When this option is enabled a thumbnail will be saved
        in the exported image as part of the EXIF metadata. This will also cause
        certain EXIF tags to be saved, even if you have disabled saving
        EXIF metadata.
      </para>
    </listitem>
  </varlistentry>
</variablelist>
