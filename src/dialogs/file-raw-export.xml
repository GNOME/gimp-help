<!DOCTYPE sect2 PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
                       "http://www.docbook.org/xml/4.3/docbookx.dtd">

<sect2 id="file-raw-export">
  <title>Export Image as Raw image data</title>

  <indexterm>
    <primary>aa</primary>
  </indexterm>
  <indexterm>
    <primary>Export Image as Raw image data</primary>
  </indexterm>
  <indexterm>
    <primary>Formats</primary>
    <secondary>Raw image data</secondary>
  </indexterm>

  <sect3>
    <title>Overview</title>
    <figure>
      <title>The Raw image data Export dialog</title>
      <mediaobject>
        <imageobject>
          <imagedata format="PNG"
            fileref="images/dialogs/export-raw-dialog.png"/>
        </imageobject>
      </mediaobject>
    </figure>

    <para>
      Raw image data means that the data of an image, i.e. pixels or palette
      indexes and a palette, is written directly to a file, without further
      information that exactly describes the organization of the image data.
    </para>
    <para>
      Raw image data export should not be confused with the RAW formats that
      some digital cameras use for high quality images.
    </para>
  </sect3>

  <sect3>
    <title>Options</title>
    <variablelist>
      <varlistentry>
        <term>Planar configuration</term>
        <listitem>
          <para>
            This setting determines how the color channels will be written
            to file.
          </para>
          <variablelist>
            <varlistentry>
              <term>Contiguous</term>
              <listitem>
                <para>
                  The image data will be written as contiguous pixels.
                </para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>Planar</term>
              <listitem>
                <para>
                  The image data will be written as separate channels or planes,
                  i.e. all the red values for each pixel are written first, then
                  blue, and finally the green and possibly alpha values.
                </para>
              </listitem>
            </varlistentry>
          </variablelist>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Palette's layout</term>
        <listitem>
          <para>
            Only enabled for indexed images. It determines how the
            palette will be written.
          </para>
          <variablelist>
            <varlistentry>
              <term>R, G, B (normal)</term>
              <listitem>
                <para>
                  Each palette entry consists of a red, green and blue value,
                  in that order.
                </para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>B, G, R, X (BMP style)</term>
              <listitem>
                <para>
                  Each palette entry consists of a blue, green, red and
                  undefined value, in that order. This is the format used
                  in Windows BMP files.
                </para>
              </listitem>
            </varlistentry>
          </variablelist>
        </listitem>
      </varlistentry>
    </variablelist>
  </sect3>
</sect2>
