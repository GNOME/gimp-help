<!DOCTYPE sect2 PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
                       "http://www.docbook.org/xml/4.3/docbookx.dtd">

<sect2 id="file-svg-export">
  <title>Export Image as SVG</title>

  <indexterm>
    <primary>Export Image as SVG</primary>
  </indexterm>
  <indexterm>
    <primary>Formats</primary>
    <secondary>svg</secondary>
  </indexterm>

  <sect3>
    <title>Overview</title>

    <figure>
      <title>The SVG Export dialog</title>
      <mediaobject>
        <imageobject>
          <imagedata format="PNG"
            fileref="images/dialogs/export-svg-dialog.png"/>
        </imageobject>
      </mediaobject>
    </figure>

    <para>
      <ulink url="https://en.wikipedia.org/wiki/SVG">Scalable Vector
      Graphics (SVG)</ulink> is a vector graphics format, allowing to export
      your image as vectors. Raster layers can be included too if desired.
    </para>
  </sect3>

  <sect3>
    <title>Options</title>

    <variablelist>
      <varlistentry>
        <term>Embed raster layers as</term>
        <listitem>
          <para>
            Select how raster layers should be included.
          </para>
          <variablelist>
            <varlistentry>
              <term>PNG (always)</term>
              <listitem>
                <para>
                  All raster layers will be included in the SVG as
                  <link linkend="file-png-export">PNG</link> images.
                </para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>JPEG (for opaque images, PNG otherwise)</term>
              <listitem>
                <para>
                  Raster layers will be included in the SVG as
                  <link linkend="file-jpeg-export">JPEG</link> images when
                  they are opaque, and as PNG when there is transparency.
                </para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>Do not export raster layers</term>
              <listitem>
                <para>
                  All raster layers will be ignored when exporting.
                </para>
              </listitem>
            </varlistentry>
          </variablelist>
        </listitem>
      </varlistentry>
    </variablelist>
  </sect3>

</sect2>
