<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
                       "http://www.docbook.org/xml/4.3/docbookx.dtd">
<!-- section history:
  2009-08-07 ude: moved ghostscript instructions here from File/Print
  2009-02-19 j.h: revised for v2.6
  2007-03-14 lexa: split out from another file
-->
<sect1 id="gimp-using-fileformats-opening">
  <title>Opening Images</title>

  <indexterm>
    <primary>Files</primary>
    <secondary>Open</secondary>
  </indexterm>

  <indexterm>
    <primary>Image</primary>
    <secondary>Open</secondary>
  </indexterm>

  <para>
    There are several ways of opening an existing image in
    <acronym>GIMP</acronym>:
  </para>

  <variablelist>
    <varlistentry id="gimp-using-open-file">
      <term>Open Image dialog</term>
      <listitem>
        <para>
          The most obvious way to open an existing image is the <guilabel>Open
          Image</guilabel> dialog.
          Use
          <menuchoice>
            <guimenu>File</guimenu>
            <guimenuitem>Open…</guimenuitem>
          </menuchoice>,
          or <keycombo><keycap>Ctrl</keycap><keycap>O</keycap></keycombo>
          to open the <link linkend="gimp-open-dialog">Open Image</link> dialog.
          This dialog allows you to navigate to the image you want to use,
          select its name, and then use the <guibutton>Open</guibutton> button
          to load the image.
          For details on how to use this dialog, see the above link.
        </para>
        <para>
          The <guilabel>Open Image</guilabel> dialog provides several features
          to help you navigate quickly to a file. Perhaps the most important is
          the ability to create <quote>bookmarks</quote>, for folders that you
          use often.
        </para>
      </listitem>
    </varlistentry>

    <varlistentry id="gimp-using-open-location">
      <term>Open Location</term>
      <listitem>
        <para>
          You can directly open an image from an online or network location if
          you know the <acronym>URI</acronym> (i.e., a web or network address)
          for the image.
          Select
          <menuchoice>
            <guimenu>File</guimenu>
            <guimenuitem>Open Location…</guimenuitem>
          </menuchoice>
          from the main menu. For more details, see the
          <link linkend="gimp-file-open-location">Open Location…</link>
          documentation.
        </para>
      </listitem>
    </varlistentry>

    <varlistentry id="gimp-using-open-recent">
      <term>Open Recent</term>
      <listitem>
        <para>
          To open an image that was recently used in
          <acronym>GIMP</acronym>, select
          <menuchoice>
            <guimenu>File</guimenu>
            <guimenuitem>Open Recent</guimenuitem>
          </menuchoice> from the main menu.
          This displays a submenu of the most recently opened images that you
          can click to open the desired image. At the bottom of this submenu is
          the <link linkend="gimp-document-dialog">Document History</link>
          command if you need to reopen an image from longer ago.
          See also <link linkend="gimp-file-open-recent">Open Recent</link>.
        </para>
      </listitem>
    </varlistentry>

    <varlistentry id="gimp-using-file-manager">
      <term>File Manager</term>
      <listitem>
        <para>
          If you have associated an image file type with
          <acronym>GIMP</acronym>,
          either when you installed <acronym>GIMP</acronym> or later, then you
          can navigate to the file using a file manager (such as Nautilus or
          Konqueror in Linux, or Windows Explorer in Windows), and once you
          have found it, double-click on the file. If properly configured,
          the image will open in <acronym>GIMP</acronym>. Most file managers
          also support drag and drop (see below).
        </para>
      </listitem>
    </varlistentry>

    <varlistentry id="gimp-using-drag-and-drop">
      <term>Drag and Drop</term>
      <listitem>
        <para>
          You can <quote>Drag and drop</quote> one or more images onto the
          <link linkend="gimp-concepts-toolbox">Toolbox</link>, or, if you
          haven't opened any images yet, the empty image window, to open the
          dragged image or images.
        </para>
        <para>
          You can also drag one or more images into an open
          <acronym>GIMP</acronym> image. The dropped image or images will be
          added to the open image as a new layer, or set of layers.
        </para>
        <para>
          Many applications support dragging and dropping an image into
          <acronym>GIMP</acronym>; for example, drag an image from
          <application>Firefox</application> and drop it onto
          <acronym>GIMP</acronym>'s toolbox.
        </para>
      </listitem>
    </varlistentry>

    <varlistentry id="gimp-using-open-copy-and-paste">
      <term>Copy and Paste</term>
      <listitem>
        <para>
          Use
          <menuchoice>
            <guimenu>File</guimenu>
            <guisubmenu>Create</guisubmenu>
            <guimenuitem>From Clipboard</guimenuitem>
          </menuchoice>
          (see <link linkend="gimp-file-create-clipboard">From Clipboard</link>)
          to create a new image from the clipboard; alternatively, you can use
          <menuchoice>
            <guimenu>Edit</guimenu>
            <guisubmenu>Paste as</guisubmenu>
            <guimenuitem>Paste as New Image</guimenuitem>
          </menuchoice>
          (see <link linkend="gimp-edit-paste-as-new-image">Paste as New
          Image</link>).
          Many applications support copying an image to the clipboard that can
          then be pasted into <acronym>GIMP</acronym>. Many operating systems
          support copying screen content to the clipboard.
        </para>
        <para>
          <keycap>Print Screen</keycap> typically copies the screen content to
          the clipboard. Print screen is not universally
          supported, and just because your operating system can copy an image to
          the clipboard, does not mean that <acronym>GIMP</acronym> can use
          the image from the clipboard. Your best bet is to try it and see
          if it works.
        </para>
      </listitem>
    </varlistentry>

    <varlistentry id="gimp-using-image-browser">
      <term>Image Browser</term>
      <listitem>
        <para>
          There are many image management applications that can be used to
          browse and manage your images. They usually work similar to a normal
          file browser in that you can double click images to load them in
          their default application; or you can use drag-and-drop to drop one
          or more images on <acronym>GIMP</acronym>'s toolbox.
        </para>
        <para>
          Example applications on Linux are:
          <application>gThumb</application>; see the gThumb website
          <xref linkend="bibliography-online-gthumb"/>. Another similar
          application is Geeqie <xref linkend="bibliography-online-geeqie"/>.
        </para>
      </listitem>
    </varlistentry>
  </variablelist>
</sect1>
