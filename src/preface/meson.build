# /src/preface/ xml files

#FIXME: We need this here because the output of a custom
# target cannot be used in a foreach loop, which is what
# we are doing while generating localized xml files
# with gen.process

xml_sources_preface = files(
    'authors.xml',
)
